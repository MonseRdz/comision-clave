  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

F  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

R  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

@  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

k  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

-  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

@  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

S  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

S  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

x  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

I  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

@  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

P  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

@  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

P  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

T  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

P  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

B  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

E  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

S  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

T  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

@  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

x  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

R  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

F  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

R  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

h  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

[  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

P  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

|  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

T  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

ó  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

ó  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

y  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

y  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

P  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

|  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

T  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

ó  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

y  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

D  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

h  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

y  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

ó  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

]  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

P  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

P  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

E  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

V  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

S  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

[  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

F  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

]  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

S  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

I  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

[  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

0  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

]  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

?  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

?  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

?  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

[  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

0  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

]  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

?  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

?  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

I  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

[  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

0  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

]  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

?  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

?  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

?  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

[  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

]  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

S  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

[  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

E  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

]  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

S  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

D  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

D  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

&  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

&  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

V  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

?  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

I  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

|  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

|  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

D  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

!  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

P  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

N  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

-  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

4  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

T  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

P  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

T  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

P  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

D  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

?  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

E  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

D  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

y  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

ó  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

S  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

D  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

ó  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

P  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

R  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

F  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

E  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

D  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

N  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

!  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

I  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

|  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

|  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

!  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

|  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

|  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

!  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

I  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

!  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

N  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

F  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

|  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

|  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

0  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

E  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

y  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

I  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

I  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

I  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

I  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

I  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

E  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

[  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

]  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

I  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

I  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

ó  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

`  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

$  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

x  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

$  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

$  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

?  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

j  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

$  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

?  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

`  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

`  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

$  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

x  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

$  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

$  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

?  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

—  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

$  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

?  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

`  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

F  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

N  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

-  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

4  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

-  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

4  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

?  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

P  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

T  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

P  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

-  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

h  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

M  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

y  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

T  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

P  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

S  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

N  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

-  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

3  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

-  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

-  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

4  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

-  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

q  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

E  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

-  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

S  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

-  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

I  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

h  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

F  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

I  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

k  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

y  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

S  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

q  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

R  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

-  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

S  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

-  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

h  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

F  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

k  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

y  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

S  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

q  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

M  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

M  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

X  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

N  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

-  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

E  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

-  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

y  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

1  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

0  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

0  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

1  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

h  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

F  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

q  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

-  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

S  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

-  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

I  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

h  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

F  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

I  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

k  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

y  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

S  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

N  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

:  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

-  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

-  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

4  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

B  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

y  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

B  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

P  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

P  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

T  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

P  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

-  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

h  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

P  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

T  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

P  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

T  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

[  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

E  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

R  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

P  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

R  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

]  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

A  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

ú  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

h  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

y  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

"  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

I  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

I  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

&  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

&  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

&  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

&  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

g  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

M  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

X  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

N  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

0  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

k  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

y  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

I  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

?  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

x  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

x  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

x  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

,  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

c  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

{  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

f  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

(  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

u  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

=  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

p  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

s  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

I  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

?  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

.  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

o  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

m  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

C  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

t  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

r  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

T  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

b  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

P  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

a  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

n  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

e  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

l  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

<  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

/  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

d  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

i  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

v  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

>  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

   async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

)  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

;  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

}  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }


  async function asignar(ev: React.FormEvent) {
    ev.preventDefault();
    const monto = Number(f.monto);
    if (!f.eventoId || !f.rubro || !f.responsableId) return setAviso("Completa todos los campos.");
    if (!Number.isFinite(monto) || monto <= 0) return setAviso("El monto debe ser mayor a cero.");
    const evento = estado.eventos.find((e) => e.id === f.eventoId);
    const resp = estado.usuarios.find((u) => u.id === f.responsableId);
    try {
      const guardado = await insertarPresupuesto({
        id: nuevoId("b"),
        eventoId: f.eventoId,
        rubro: f.rubro,
        monto,
        responsableId: f.responsableId,
      });
      setEstado((e) => ({ ...e, presupuestos: [...e.presupuestos, guardado] }));
      await registrar(
        "Asignación de presupuesto",
        `${mxn(monto)} al rubro ${f.rubro} de ${evento?.nombre} bajo ${resp?.nombre}.`,
      );
      setError("");
      setAviso(`Asignados ${mxn(monto)} a "${f.rubro}" en ${evento?.nombre} — responsable ${resp?.nombre}.`);
      setF({ ...f, monto: "" });
    } catch (err: unknown) {
      setAviso("");
      setError(err instanceof Error ? err.message : String(err));
    }
  }

